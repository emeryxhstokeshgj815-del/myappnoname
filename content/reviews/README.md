# Review logs

One file per batch (`bNNN.md`), written by the second editor following `content/REVIEW.md`.
Entries removed during review are collected in `removed.json` and excluded by
`scripts/build_dataset.py`.
